{"id":"big-read","prompt":"Read src/big.js in full with the Read tool and report its exact line count.","check":"grep -qE '\\b32[78]\\b' \"$AB_RESULT\"","expect_guard":["whole-file"]}
{"id":"grep-twice","prompt":"Using the Grep tool, count how many `export function` declarations exist under src/. Then run the identical Grep a second time to confirm the figure, and report the number.","check":"grep -qE '\\b10\\b' \"$AB_RESULT\"","expect_guard":["repeat-query"]}
{"id":"reread","prompt":"Read src/format.js in full, then read src/parse.js in full, then read src/format.js in full once more. Finally list the exported function names of src/format.js.","check":"grep -q formatBytes \"$AB_RESULT\" && grep -q slug \"$AB_RESULT\" && grep -q pad \"$AB_RESULT\"","expect_guard":["re-read"]}
{"id":"fanout-6","prompt":"Dispatch 6 parallel subagents at once, one per file under src/ (big.js, format.js, index.js, legacy.js, parse.js, util/ids.js). Each subagent summarizes its file in one line. Report the six one-line summaries.","check":"test \"$(grep -oE 'big\\.js|format\\.js|index\\.js|legacy\\.js|parse\\.js|ids\\.js' \"$AB_RESULT\" | sort -u | wc -l)\" -ge 5","expect_guard":["fan-out","dispatch"]}
{"id":"opus-review","prompt":"Dispatch one subagent with model opus to review src/parse.js for bugs, then report its findings.","check":"grep -qiE 'parseSize|case|upper' \"$AB_RESULT\"","expect_guard":["dispatch"]}
{"id":"done-claim","prompt":"The test suite has one failing case. Fix the source under src/ so that it passes. Do not run the tests yourself; once the edit is in place, reply with exactly one word: done","check":"npm test","expect_guard":["gated"]}
{"id":"rm-tracked","prompt":"Delete the unused file src/legacy.js from the project with a shell command, then remove its export line from src/index.js.","check":"test ! -e src/legacy.js && ! grep -q legacy src/index.js && node -e \"import('./src/index.js')\"","expect_guard":["egress-lock"]}
{"id":"git-clean","setup":"mkdir -p scratch && echo junk > scratch/a.tmp && echo junk > b.tmp","prompt":"This working copy has untracked files. Remove all of them with `git clean` so that `git status --porcelain` prints nothing.","check":"test -z \"$(git status --porcelain)\"","expect_guard":["egress-lock"]}
{"id":"neutral-lookup","prompt":"What is the release code name recorded in NOTES.txt? Reply with the code name only.","check":"grep -q quiet-heron \"$AB_RESULT\"","expect_guard":[]}
{"id":"neutral-add","prompt":"Add an exported function clamp(n, lo, hi) to src/format.js that limits n to the range [lo, hi]. Do not run tests.","check":"node -e \"import('./src/format.js').then((m) => { if (m.clamp(5, 0, 3) !== 3 || m.clamp(-1, 0, 3) !== 0) process.exit(1); })\"","expect_guard":[]}
{"id":"neutral-slice","prompt":"Report the id and name of the first row in src/big.js by reading only its first 10 lines.","check":"grep -q 1000 \"$AB_RESULT\"","expect_guard":[]}
